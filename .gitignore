node_modules/
.env

# /srv/chat is this repo's clone on the LXC and also its runtime directory, so
# the paths the deploy creates there sit inside the working tree. Ignored so a
# future upstream file at the same path cannot make git pull refuse.
/data/
/claude/
/.env.image
.planning
